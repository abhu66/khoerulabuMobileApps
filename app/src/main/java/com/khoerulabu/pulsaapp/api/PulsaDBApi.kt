package com.khoerulabu.pulsaapp

object PulsaDBApi {
    //deklarasi url dan link api yang akan digunakan
    private var BASE_URL                        = BuildConfig.BASE_URL
    fun baseUrl()                               = "$BASE_URL/"
}